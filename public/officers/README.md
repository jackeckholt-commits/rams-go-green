# Officer photos

Add JPG, PNG, or WebP photos to this folder. For example:

- `president.jpg`
- `vice-president.jpg`
- `treasurer.jpg`

Then open `content/site.json` and set the matching officer's `photo` value to
`/officers/president.jpg` (or the filename you used).
